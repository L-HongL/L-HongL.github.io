---
title: "C2"
---

```cpp
#include<bits/stdc++.h>
using namespace std;
#define LL long long
#define pii pair<int,int>
#define pll pair<LL,LL>
#define pu push_back
#define pk pop_back
#define all(a) (a).begin(),(a).end()
#define vii vector<int>
#define vll vector<LL>
#define se second
#define fi first
const LL mod=1e9+7;
struct edg{
    LL x;
    LL k;
    LL i; 
    bool operator<(const edg& other) const{
        return x<other.x;
    }
};

void babason(){    
    LL ans=1e18;
    LL ax,n;cin>>ax>>n;
    LL axx=ax;
    
    vector<int> a(n+1,0);
    vector<int> axs;
    while(axx){
        axs.pu(axx%10);
        axx/=10;
    }
    axs.pu(0);reverse(all(axs));
    for(int i=1;i<=n;i++){
        cin>>a[i];
    }

    int m=axs.size()-1;
//     vector<vll> dpmin(17+3,vll(n+1,0));
//     LL midmin=0;
//     for(int i=1;i<axs.size();i++){
//         midmin=midmin*10+axs[i];
//         for(int j=1;j<=n;j++){
//             for(int k=1;k<=n;k++){
//                 LL mid=dpmin[i-1][k]*10+a[j];
//                 if(midmin-mid>=0){
//                     dpmin[i][j]=max(dpmin[i][j],mid);
//                 }
//             }
//         }
//     }
//     for(int i=1;i<=n;i++){
//         if(abs(ans-ax)>abs(dpmin[m][i]-ax)){
//             ans=dpmin[m][i];
//         }
//     }

    for(int ii=0;ii<=n;ii++){
        vector<vll> dpmax(17+3,vll(n+1,1e19));
        for(int i=0;i<=n;i++){
            dpmax[0][i]=a[i];
        }
        LL midmax=0;
        for(int i=1;i<axs.size();i++){
            midmax=midmax*10+axs[i];
            for(int j=1;j<=n;j++){
                for(int k=1;k<=n;k++){
                    LL mid=dpmax[i-1][k]*10+a[j];//cout<<mid<<"\n";
                    if(mid-midmax>=0){//cout<<dpmax[i][j]<<" "<<i<<"\n";
                        dpmax[i][j]=min(dpmax[i][j],mid);
                    }
                }
            }
        }
        for(int i=1;i<=n;i++){
            if(abs(ans-ax)>abs(dpmax[m][i]-ax)){
                ans=dpmax[m][i];
                //cout<<dpmax[m][i]<<" "<<i<<"\n";
            }
        }
    }

    cout<<ans<<'\n';
    
}
int main(){
    cin.tie(0);
    cout.tie(0);
    ios::sync_with_stdio(0);   
    int t;cin>>t;while(t--)
    babason();
    return 0;
}
```